Clone this in your home directory:

```bash
cd ~ && git clone git@github.com:nomaitech/.agents.git
```

```bash
cd ~/.agents
ln -s ~/.agents/AGENTS.md ~/.claude/CLAUDE.md
npx github:jonbesga/skills experimental_install
mkdir ~/.claude/skills && ln -s ~/.agents/skills/* ~/.claude/skills/
```
